/**
 * 附件引用块。
 *
 * 带附件发送时，附件路径会以固定格式追加到 prompt 末尾——模型要靠这段文本才知道去哪读文件。
 * 但这段是写给模型的，界面上应该还原成附件卡片而不是原样显示，所以生成和解析放在同一个文件里，
 * 改格式时两边不会走散；历史会话里存的也是这个格式，解析必须一直兼容。
 */
export * from "../attachments/references.js";
